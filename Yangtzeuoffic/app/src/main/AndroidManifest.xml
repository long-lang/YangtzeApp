<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sweethearts">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- SD卡写入权限 写入扩展存储，向扩展卡写入数据 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.FullScreen"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activity.LoginActivity"/>
        <activity android:name=".ui.activity.base.BaseActivity" />
        <activity android:name=".ui.activity.ChartActivity"/>
        <activity android:name=".ui.activity.ImageActivity"/>
        <activity android:name=".ui.activity.JwcListActivity"/>
        <activity android:name=".ui.activity.TripActivity"/>
        <activity android:name=".ui.activity.NewsDetailsActivity"/>
        <activity android:name=".ui.activity.WebActivity"/>
        <activity android:name=".ui.activity.CetActivity"/>
        <activity android:name=".ui.activity.PingJiaoActivity"/>
        <activity android:name=".ui.activity.ChooseClassActivity"/>
        <activity android:name=".ui.activity.ChangePassActivity"/>
        <activity android:name=".ui.activity.TestActivity"/>
        <!--下面这个没实现-->
        <activity android:name=".ui.activity.MessageActivity"/>
        <activity android:name=".ui.activity.LaucherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>h
        <activity android:name=".ui.activity.MainActivity" />
    </application>

</manifest>